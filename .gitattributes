# Fleet-baseline line-ending normalization: LF for all text, CRLF for PowerShell.
# (eol=lf still covers *.py, preserving the ruff-format LF requirement; core.autocrlf
# no longer fights checkout because every text type is normalized the same way.)
* text=auto eol=lf
*.ps1 text eol=crlf

# eval/

CLI `--help` regression snapshots + eval history + ontology benchmark; baseline for CLI/doc drift (`scripts/eval.py`). Snapshots captured 2026-03-28 — re-capture before trusting as a live gate.

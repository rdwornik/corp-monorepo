# Audit — ADR-27 Implementation Status (2026-05-25)

**Date:** 2026-05-25
**Branch:** `audit/adr27-status`
**Author:** Claude Code (read-only audit, file-state verified)
**Subject:** `corp-monorepo`
**Method:** read-only `git ls-files` / `git log` / `git merge-base` / `grep` / file reads; every claim cites verbatim stdout or a file path. No execution, no source modifications.

---

## 1. Verification context

- **HEAD SHA:** `32a47f85b07d697be20066c1ec69df3cf92cb1f6` — matches the handoff bundle's expected SHA; audit proceeds against the intended tree.
- **Branch:** `audit/adr27-status`, cut from `main` at HEAD.
- **Date:** 2026-05-25.
- **Generated by:** Claude Code, read-only verification during plan-mode investigation, then written here.
- **Why this audit exists:** The prior session's handoff conflated two independent questions about ADR-27 — (Q1) is the ADR doc merged into `main`, and (Q2) is the ADR-mandated OneDrive implementation present. Operator memory said "drafted-on-branch as of 2026-04-22, no merge witness"; the handoff's Stage-3 table said VERIFIED present. This audit resolves both empirically.

---

## 2. Question 1 — ADR doc merge status

### Evidence (verbatim stdout)

```
$ git ls-files docs/decisions/ADR-27*
docs/decisions/ADR-27-safety-invariants.md

$ git ls-files docs/decisions/DECISION_27*
(empty)

$ git log --all --oneline -- docs/decisions/ADR-27-safety-invariants.md
a5a6789 docs: resolve ADR-27 number collision (transcript to transcripts/)
a6e3942 docs(adr): ADR-27 safety invariants (OneDrive centralization + vault writer amendment)

$ git merge-base --is-ancestor a6e3942 main  → YES (in main history)
$ git merge-base --is-ancestor a5a6789 main  → YES (in main history)
```

The `DECISION_27*` glob returned empty because the transcript was moved under `transcripts/` —
it lives at `docs/decisions/transcripts/DECISION_27_onedrive_centralization.md` (commit `a5a6789`
"resolve ADR-27 number collision (transcript to transcripts/)"). The directive's cited path
`docs/decisions/DECISION_27_onedrive_centralization.md` is therefore slightly stale, but the
file is present in the correct location.

### Interpretation

**The ADR doc is PRESENT in `main`.** Both commits touching it (`a6e3942` creation, `a5a6789`
collision fix) are confirmed ancestors of `main`. Operator memory ("drafted-on-branch, no merge
witness") was **stale**; the handoff's "VERIFIED present" claim was **correct**.

---

## 3. Question 2 — Safety code presence

### Evidence (verbatim stdout)

```
$ git ls-files src/corp/safety/
(empty)

$ git ls-files tests/safety/
tests/safety/__init__.py
tests/safety/test_vault_writer_invariant.py

$ git ls-files | grep -i safety
docs/decisions/ADR-27-safety-invariants.md
src/corp/overnight/safety.py
tests/safety/__init__.py
tests/safety/test_vault_writer_invariant.py
tests/test_actions/test_archive_onedrive_safety.py
tests/test_cleanup/test_onedrive_safety_p1_1.py
tests/test_cleanup/test_onedrive_safety_p1_2.py
tests/test_cleanup/test_onedrive_safety_symlink.py
tests/test_overnight/test_safety.py

$ grep -rn "from corp.safety" src/ tests/ --include="*.py"
(empty)
```

### Interpretation — the answer is SPLIT across ADR-27's two decisions

| Decision / PR | Mandated artifact | Status | Evidence |
|---|---|---|---|
| **Decision 2 / PR-4** (vault-writer invariant) | `tests/safety/test_vault_writer_invariant.py` + `is_writable_by_actions` predicate | **DONE** | File tracked; JOURNAL 2026-05-18 documents the 448-line AST scanner + predicate; landed before this audit. |
| **Decision 1 / PR-1** (foundation module) | `src/corp/safety/onedrive.py` | **ABSENT** | `git ls-files src/corp/safety/` empty; zero `from corp.safety` imports anywhere. |
| **Decision 1 / PR-2** (guard migration + renderer + deck_actions) | callers unified on `guard_path`/`OneDriveSafetyError` | **ABSENT** | Guards still import from `corp.cleanup.errors`; `renderer.py` still raises `ValueError` (§5). |
| **Decision 1 / PR-3** (CI enforcement) | `tests/safety/test_no_unguarded_writes.py` AST scanner | **ABSENT** | Not in `tests/safety/` listing. |

The OneDrive guards that *do* exist are the **hotfix-era** implementation (`hotfix/onedrive-safety-p1`,
merged 2026-04-21): inline resolve-then-substring guards importing `OneDriveSafetyError` from
`src/corp/cleanup/errors.py`. The ADR-27 *centralization* (Decision 1) has not been built.

---

## 4. Deep inspection findings

| File | Import source | Drift status | Guard pattern |
|---|---|---|---|
| `src/corp/cleanup/errors.py` | — (defines `OneDriveSafetyError`, `PathTraversalError`) | Error classes live here, **not** ADR-mandated `corp/safety/onedrive.py` | Definition site |
| `src/corp/cleanup/disk.py:18,46,52` | `from corp.cleanup.errors import OneDriveSafetyError` | Hotfix location (pre-centralization) | Inline, fail-closed, raises `OneDriveSafetyError` |
| `src/corp/cleanup/executor.py:15,37,43` | `from corp.cleanup.errors import OneDriveSafetyError, PathTraversalError` | Hotfix location | Inline, fail-closed |
| `src/corp/actions/_helpers.py:13,123,129` | `from corp.cleanup.errors import OneDriveSafetyError` | Hotfix location | Inline, fail-closed |
| `src/corp/project/renderer.py:42,48` | — (raises bare `ValueError`) | **Documented drift** — comment lines 36–37 mark it deliberate, pending PR-2 | Raises `ValueError`, not `OneDriveSafetyError` |
| `src/corp/actions/deck_actions.py:104→117` | none | **Undocumented gap** — known unguarded write-intent caller | No guard before `copy_template(...)` write |

No file anywhere imports `from corp.safety` — confirming the foundation module does not exist and
nothing depends on it yet.

---

## 5. Verified-present

- `docs/decisions/ADR-27-safety-invariants.md` — in `main` (commits `a6e3942`, `a5a6789`).
- `docs/decisions/transcripts/DECISION_27_onedrive_centralization.md` — Council transcript, in `main` (`a5a6789`).
- `tests/safety/test_vault_writer_invariant.py` + `tests/safety/__init__.py` — Decision 2 / PR-4 enforcement (`git ls-files tests/safety/`).
- `src/corp/cleanup/errors.py` — `OneDriveSafetyError` + `PathTraversalError` definitions (hotfix location).
- Hotfix guard sites: `src/corp/cleanup/disk.py`, `src/corp/cleanup/executor.py`, `src/corp/actions/_helpers.py` (all `grep`-confirmed, importing from `corp.cleanup.errors`).
- `docs/audits/2026-04-21-codex-hotfix-review.md` — present (`git ls-files docs/audits/`).

## 6. Verified-absent

- `src/corp/safety/onedrive.py` — `git ls-files src/corp/safety/` empty. (Decision 1 / PR-1.)
- `tests/safety/test_no_unguarded_writes.py` — not in `tests/safety/` listing. (Decision 1 / PR-3.)
- Any `from corp.safety` import — `grep` empty across `src/` and `tests/`. (Decision 1 / PR-2.)
- `docs/audits/2026-04-21-p1-verification.md` — cited in ADR §Related-work line 179 as "Verification evidence," **not** in `git ls-files docs/audits/`. (See §9.)

---

## 7. Drift list

Three divergences between ADR-27's stated plan and current code, separated by remediation profile:

| # | Drift site | Type | Remediation profile |
|---|---|---|---|
| D1 | `renderer.py:42,48` raises `ValueError` (not `OneDriveSafetyError`) | **Documented intent** — comment at lines 36–37 explicitly marks it deliberate pending PR-2 (the two CLI callers catch `(FileNotFoundError, ValueError)`) | Remediated when **PR-2** migrates callers to the foundation module and updates the two `ValueError`-catching CLI callers. Tracked by the ADR; low surprise risk. |
| D2 | `deck_actions.py:104→117` — `_resolve_project_path(...)` result written via `copy_template(...)` with no OneDrive guard | **Undocumented gap** — no comment, not on any current PR's written scope beyond a single mention in ADR §Decision 1 prose | **Must be explicitly added to PR-1 or PR-2 planning.** ADR §Decision 1 names it ("`deck_actions.py:104` remains a known unguarded write-intent caller") and PR-2's row says "Fix `deck_actions.py:104`", but it is the only drift that would **survive a "PR-2 done" closure undetected** if the PR-2 checklist drops it. Flag at PR planning. |
| D3 | `OneDriveSafetyError`/`PathTraversalError` in `cleanup/errors.py`, not `safety/onedrive.py` | **Structural drift** — error classes and all guards sit at the hotfix location, not the ADR foundation layer | Resolved mechanically by **PR-1** (create foundation module) + **PR-3** (caller migration / re-export cleanup). The ADR plans a one-release re-export shim from the four existing sites before deletion. |

---

## 8. Routing decision

**Branch (a) fires** — ADR doc PRESENT in `main`, canonical OneDrive safety code ABSENT.

Refinement grounded in the §3 evidence: ADR-27 has **two** decisions, and Decision 2 / PR-4
(vault-writer invariant) is already implemented and merged (`tests/safety/test_vault_writer_invariant.py`,
JOURNAL 2026-05-18). What remains is **Decision 1's OneDrive centralization (PR-1 → PR-2 → PR-3)**.
The routing is therefore not "implement ADR-27 from scratch" but "execute the three outstanding
OneDrive PRs, starting at PR-1."

Rationale: `git ls-files src/corp/safety/` is empty and no module imports `corp.safety`, so the
foundation layer the other two PRs depend on does not yet exist — PR-1 is unblocked and is the
correct next unit of work. PRs 2 and 3 are gated on PR-1 per the ADR migration plan.

## 9. Recommended next action

**Next session: ADR-27 Decision 1, PR-1 (Foundation).** Scope per ADR §Migration plan:

1. Create `src/corp/safety/onedrive.py` at the Tach **foundation** layer with the documented public
   API (`OneDriveSafetyError`, `is_onedrive_path`, `guard_path(path, *, reason)`,
   `@onedrive_write_exempt(reason=...)`).
2. **Verify no Tach cycle** before merge; fallback placement `corp/core/safety/` if foundation layer
   isn't exposed. (Tach is `exact=true`, 4-layer — confirm the new module's allowed importers.)
3. Enumerate third-party write libraries in use (`send2trash`, `aiofiles`, etc.) per ADR §limitation 4.
4. Grep monorepo for `except.*ValueError` in callers of `corp.project.cli.render` (PR-2 input).
5. **Explicitly carry D2 (`deck_actions.py:104`) into the PR-1 or PR-2 checklist** so it cannot be lost.
6. Codex `/review` per the high-risk-architectural-change policy.

PR-2 (guard migration + renderer unification + deck_actions fix) and PR-3 (AST scanner CI enforcement)
follow, each gated on the prior merge.

## 10. Open questions

- **`docs/audits/2026-04-21-p1-verification.md` is absent from `main`** despite being cited in
  ADR-27 §Related-work (line 179) as the hotfix verification evidence. The companion
  `2026-04-21-codex-hotfix-review.md` IS present. Either the verification doc was never committed,
  was lost, or the ADR citation is aspirational. Not resolvable from `git ls-files` alone — needs a
  decision from Rob (recover/recreate, or correct the ADR citation). This was the handoff Stage-3
  open question; this audit confirms the file's absence but cannot recover its content.
- **No other ADR-27 items are unresolved by `git ls-files`.** Both routing questions are
  empirically answered above.

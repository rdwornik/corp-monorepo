# HANDOFF — corp-monorepo

**Date:** 2026-03-30 → 2026-04-15 (multi-day session)
**Verified:** 2026-04-17 (tach check clean, git log, tach.toml module count, docs/decisions/ list)
**Project Scale:** L (multi-package monorepo, 2495+ tests, ARCHITECTURE.md, per-module READMEs, AGENTS.md)

## OBJECTIVE

Complete structural overhaul of corp-monorepo — clean architecture fixes, Codex code review integration, stale reference cleanup, TODO audit, C4 diagram strategy (Council #25), Tach import boundary enforcement (Council #26), and documentation reconciliation to single 4-layer taxonomy.

## STATUS

All structural, enforcement, and documentation work complete. Codebase is architecturally clean, Tach-enforced, documentation-consistent. **Pipeline UNVERIFIED end-to-end since Council #24 MyWork restructure — this is the #1 priority for next session.**

## COMPLETED

### Clean architecture — 6/6 violations addressed (5 RESOLVED, 1 ACCEPTED)

- Circular import `llm_router` ↔ `intent_router` → `routing_types.py` extracted at L2
- God class `OpsDB` 705 LOC → 5 per-entity repositories (asset, package, event, routing, suggestion). OpsDB stays as facade.
- God module `built_in_actions.py` 967 LOC → `actions/` package with 12 domain modules. Re-export shim for backward compat.
- Mixed concerns `inbox.py` → `inbox_ops.py` (7 pure logic functions extracted)
- Long functions `extract_from_text()` 203L → Strategy pattern (PDFMultimodal, PPTXMultimodal, TextFallback). `extract.py` 1589→1184 LOC (-405)
- CLI depth L9 → ACCEPTED (static L3, runtime L9 via lazy imports, each layer justified)

### Codex code review integration

- `AGENTS.md` created in repo root — review checklist (6 critical, 6 high, 6 medium, 3 low), severity calibrated after first audit, two review modes (diff/full audit), read-only commands explicitly allowed
- First full Codex audit: 4 CRITICAL + 4 HIGH → 6 actionable items fixed, 3 deferred as ACCEPTED limitations

### Codex audit fixes (6/6 actioned)

- REAL BUG: `rfp_only` filter dropped during product expansion in `retrieve/engine.py:142`
- `naming_config.py` moved from `ingest/` to `schema/` (layer violation, shim left for backward compat)
- `schema/cli.py` `--in-place` deprecated (sole-writer invariant violation)
- OneDrive safety guard added to `project/renderer.py`
- `vault_root` path resolution fixed in `retrieve/engine.py`
- POSIX paths (`.as_posix()`) in `project/manifest_generator.py`
- ACCEPTED: `synthesize.py` staging writes, `rfp_feedback.py` atomicity, CLI return annotations

### Stale package references purged

- 3 broken runtime paths fixed (`overnight/cke_client.py`, `project/cke_invoker.py`, `scripts/extract_training_data.py`)
- `config/agents.yaml` agent names updated (corp-knowledge-extractor → extractor, etc.)
- CLAUDE.md module layout updated, ~25 source docstrings updated

### TODO audit

- Zero real TODOs found (all false positives: `TaskStatus.TODO` enum, "hacks" as noun, XXXX in JSON templates)
- `corp-monorepo.code-workspace` configured with TODO Tree exclusions for frozen docs

### Council #25 — Diagram strategy (implemented)

- Decision: Mermaid source (`.mermaid`) + generated SVG (`.svg`) in `docs/diagrams/`
- 3 diagrams: system-context, container-module, magistrala-pipeline
- `scripts/render-diagrams.ps1` created (has Join-Path bug line 5 — needs nested Join-Path)
- Key finding: SVG image refs in markdown do NOT render in VS Code editor mode — must open `.svg` directly
- Diagrams v1 functional but visually weak (monochrome, low detail on dark theme)

### Council #26 — Tach adoption (Phase 1 + Phase 2 + Step 12)

- Tach 0.34.0 enforcing 4-layer model: foundation / core / orchestration / interface
- **32 modules classified** in `tach.toml` (current state — ADR-26 doc references 34 from Phase 1 bootstrap; 2 removed during sync)
- Pre-commit hook + GitHub Actions CI (parallel to pytest)
- `CONTRIBUTING.md` with `tach sync` as cultural rule (NOT automation)
- ADR-26 created with Phase 1 decision + Phase 2 resolution
- 6 baseline violations resolved via reclassification (zero Python changes): `corp.project_resolver` orchestration→core, `corp.query_engine` interface→orchestration, `corp.ingest` reclassified to orchestration
- Step 12: AGENTS.md, ARCHITECTURE.md, 5 READMEs reconciled from 7-layer (L0-L6) to 4-layer taxonomy
- Final Codex review: zero Critical/High findings

### Documentation updates

- `docs/ARCHITECTURE.md` — 369 lines + diagram pointers, 4-layer taxonomy throughout
- 12 per-module `README.md` files (5 updated for 4-layer taxonomy)
- Gotchas added: README staleness, diagram maintenance, TODO noise in frozen docs
- Accepted Limitations section in ARCHITECTURE.md (3 deferred Codex audit items)

## PENDING

### Priority #1 — CRITICAL

- **Magistrala verification** — end-to-end pipeline test. UNVERIFIED since Council #24 restructure. `routing_map.yaml` moved to `.corp/`, folder names changed, `naming_config` moved, stale paths fixed. Pipeline may be broken.

### Priority #2 — Documentation

- QC docs freshness — `PROMPT_qc_docs_freshness.md` ready, not yet executed
- Diagram v2 — add colors (classDef), rewrite container-module with real dependencies, improve dark theme readability
- `render-diagrams.ps1` Join-Path bug (line 5, PowerShell needs nested Join-Path)

### Priority #3 — Features

- MISC rate measurement day 7 (target <20%, was 53%)
- MyWork archive logic based on `Project_Codes.xlsm` status (NOT timestamps)
- MinHash wiring into `inbox.py`
- RFP Federation (ADR-22)
- 30-day skill eval (due 2026-04-25)

## KEY DECISIONS

- **Council #25:** Mermaid + SVG in `docs/diagrams/`. Max 3 diagrams. Edit `.mermaid` only, never `.svg`. Render via `scripts/render-diagrams.ps1`.
- **Council #26:** Adopt Tach. Pre-commit + CI enforcement. Distributed ownership with lightweight review. `tach sync` = cultural rule, NOT automation. `tests/` excluded from `source_roots`.
- **Clean architecture:** 5 structural violations resolved via re-export shim pattern (zero consumer changes across all refactors). 1 ACCEPTED. 3 Codex audit items ACCEPTED as limitations.
- **Archive logic:** NEVER use file timestamps. `Project_Codes.xlsm` Won/Loss status is authoritative.
- **TODO management:** TODOs live ONLY in active source code. Frozen docs never have TODO markers. TODO Tree excludes `docs/archive/`, `docs/decisions/`, `scripts/archive/`.
- **Taxonomy:** Single 4-layer model (foundation/core/orchestration/interface) enforced by Tach, documented everywhere. Frozen historical docs retain 7-layer references intentionally.

## CONTEXT

- **Tests:** 2,495 passing, 6 skipped
- **Council decisions:** 26 (ADR-01 through ADR-26)
- **Tach:** `tach check` clean, pre-commit + CI active, 4-layer model enforced, 32 modules in tach.toml
- **Git:** main branch, clean worktree, last commit `f120c11`
- **Safety tag:** `v-pre-consolidation` exists for rollback to 6-package structure
- **Env vars:** `PROJECT_CODES_EXCEL` = `C:\Users\1028120\Documents\MyWork\30_Reference\Project_Codes.xlsm`
- **Pipeline config:** `MyWork/.corp/routing_map.yaml` (moved from `90_System` per Council #24)
- **naming_config:** Lives in `schema/`, shim in `ingest/` for backward compat
- **corp-by-os name:** Preserved in `%LOCALAPPDATA%` paths (orphaning risk) and DB provenance fields (audit trail integrity)
- **Frozen docs:** `docs/archive/`, `docs/decisions/`, `docs/audits/` retain old 7-layer references — intentional, do not update

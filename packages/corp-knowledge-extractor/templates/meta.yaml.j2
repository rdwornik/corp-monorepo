extracted_at: "{{ extracted_at }}"
model: "{{ model }}"
pipeline_version: "{{ pipeline_version }}"
prompt_hash: "{{ prompt_hash }}"
{% if cover_slide %}cover_slide: "{{ cover_slide }}"
{% endif %}{% if cover_frame %}cover_frame: "{{ cover_frame }}"
{% endif %}source_files:
{% for f in source_files %}
  - path: "{{ f.path }}"
    type: "{{ f.type }}"
    size_bytes: {{ f.size_bytes }}
{% endfor %}
